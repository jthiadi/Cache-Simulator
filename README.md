# Cache-Simulator
This repository contains implementation of a cache simulator that reads cache configuration files and memory reference traces, then reports cache hits and misses for each reference, along with the total miss count.
